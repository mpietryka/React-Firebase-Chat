import tw, { styled } from "twin.macro";

export const Btn = styled.button(() => [
  tw`w-full mt-2 text-center px-4 py-3 bg-blue-500 rounded-md opacity-90 hover:opacity-100 transition-opacity text-white`,
]);
